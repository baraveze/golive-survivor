export const BALANCE = {
  width: 1280,
  height: 720,
  gameDurationSeconds: 90,
  playerMaxStability: 100,
  playerMoveSpeed: 240,
  playerRadius: 16,
  invulnerabilityAfterHitMs: 700,
  attackIntervalMs: 650,
  attackRange: 620,
  projectileSpeed: 640,
  projectileLifeMs: 1400,
  maxProjectiles: 16,
  hotfixCooldownMs: 8000,
  hotfixRadius: 205,
  hotfixDamage: 8,
  bossSpawnSecond: 65,
  bossWarningSeconds: 2,
  bossSpawnPauseSeconds: 5,
  maxEnemies: 100,
  spawnStartMs: 1100,
  spawnEndMs: 350,
  comboWindowMs: 2000,
  comboMultipliers: [1, 1.2, 1.5, 2, 2.5, 3],
  pointsPerSecond: 5,
  survivalBonus: 1000,
  maxScore: 100000,
  maxIssues: 1000,
} as const;
